# Production: скопируй в .env и заполни.
# Если .env нет — сервер подставит значения для локального теста (Docker MySQL на localhost).
# Для prisma db push / migrate нужен .env — скопируй этот файл в .env.

DATABASE_URL="mysql://dayflow:dayflow@localhost:3306/dayflow"
SESSION_SECRET="your-super-secret-key-change-in-production"

# Несколько origin через запятую, без пробелов
CORS_ORIGINS="https://something.com,http://localhost:5173"
PORT=4000

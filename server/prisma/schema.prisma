generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id           String    @id @default(cuid())
  email        String    @unique
  passwordHash String
  avatarUrl    String?   // null = нет аватара
  createdAt    DateTime  @default(now())

  sessions Session[]
  workspaces Workspace[]
  cards      Card[]
  tools      Tool[]
}

model Session {
  id        String   @id
  userId    String
  expiresAt DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model Workspace {
  id          String   @id @default(cuid())
  title       String
  description String?  @db.Text
  icon        String?  // один эмодзи, например "📁"
  pinned      Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  ownerId     String

  owner   User     @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  columns Column[]
  cards   Card[]
  tools   Tool[]

  @@index([ownerId])
}

model Column {
  id          String @id @default(cuid())
  title       String
  order       Int
  workspaceId String

  workspace Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  cards     Card[]

  @@index([workspaceId])
}

model Card {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  ownerId     String
  workspaceId String?  // null => "хаб" пользователя
  columnId    String?  // null => не на доске (хаб или backlog)
  order       Int?     // имеет смысл только если columnId != null

  type    CardType
  title   String?
  done    Boolean @default(false)

  payload Json
  tags    Json    @default("[]") // MySQL: массив строк как JSON

  learningStatus LearningStatus? // Новое поле: статус обучения

  owner     User       @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  workspace Workspace? @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  column    Column?    @relation(fields: [columnId], references: [id], onDelete: Cascade)

  @@index([ownerId])
  @@index([workspaceId])
  @@index([columnId])
  @@index([type])
  @@index([columnId, order])
  @@index([learningStatus])
}

model Tool {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  ownerId     String
  workspaceId String? // null => "хаб" (общий toolbox)

  title       String
  link        String?
  description String? @db.Text
  icon        String?
  tags        Json    @default("[]")

  owner     User       @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  workspace Workspace? @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([ownerId])
  @@index([workspaceId])
}

enum CardType {
  note
  link
  checklist
}

enum LearningStatus {
  want_to_repeat
  questions_remain
  deepen_knowledge
}
